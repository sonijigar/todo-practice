# Prometheus configuration for tasks-api
global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "tasks-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["localhost:8000"]
        labels:
          service: "tasks-api"
